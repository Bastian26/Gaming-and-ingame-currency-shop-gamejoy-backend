spring:
  datasource:
    #url: jdbc:postgresql://localhost:5432/postgres  # Dev: localhost
    url: jdbc:postgresql://localhost:5432/postgresDb
    username: postgresUser  # For all enviroment as env var in future
    password: secret        # For all enviroment as env var in future

springdoc:
  swagger-ui:
    path: /custom-swagger-ui  # Custom Pfad zur Swagger-UI - http://localhost:8080/custom-swagger-ui